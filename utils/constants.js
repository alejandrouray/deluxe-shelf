// ACTION NAMES

export const SET_PAGE = "SET_PAGE";
export const TOGGLE_MODAL = "TOGGLE_MODAL";

// FORM CUSTOM VALIDATIONS

export const MIN_SELECT = {
  validate: { minSelect: (value) => value.length > 0 },
};
